# Rust
target/
# Cargo.lock is committed for binary crates (reproducible builds)

# Node
node_modules/
.env
.env.*

# Build output
dist/
build/
server/static/

# Tauri
desktop/src-tauri/target/
desktop/src-tauri/WixTools/
*.app
*.dmg
*.deb
*.rpm
*.msi
*.exe

# Config with secrets (root and dev/)
config.toml
dev/config.toml

# Dev vault — tracked files are the default getting-started content.
# Add your own notes freely; git will only track what you explicitly add.

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# #ash vault metadata (not committed — lives on the mounted volume)
.mdkb/
